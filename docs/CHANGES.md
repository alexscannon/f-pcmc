# CHANGES

- A cumulative history of changes to the repo.

## 2026-07-10 — T0: Repository scaffold, config system, reference-code policy (`task/T00-scaffold`, commits `39ce798`..`054ecad`)

- **Tooling**: `pyproject.toml` with runtime deps pinned to the source project's exact versions per ASSETS §4 (`numpy==2.4.3`, `scipy==1.17.1`, `scikit-learn==1.8.0`, `umap-learn==0.5.11`) plus `pyyaml==6.0.2`; dev deps `pytest==8.4.1`, `pytest-mock==3.14.1`. `pytest.ini` registers the `slow` marker (`testpaths = tests`). `.python-version` pins 3.14.3 — verified identical to the source venv that produced `tests/reference_numbers.yaml`. `uv.lock` committed.
- **Layout**: `fpcmc/`, `eval/`, `baselines/` created as packages; `eval/`/`baselines/` are placeholders for T13/T14. Top-level-sibling reading of PRD §9 confirmed by owner (PRD's nested indentation treated as a formatting artifact).
- **Config system**: `fpcmc/config.py` — frozen `FPCMCConfig` dataclass with all PRD §8 keys and defaults (nested frozen `UmapConfig`/`HdbscanConfig`); unknown keys rejected with the offending key named (any nesting level); type-checked values; `to_yaml()`/`from_yaml()` lossless round-trip. `configs/default.yaml` transcribes PRD §8 verbatim.
- **RNG**: `fpcmc/rng.py` — single `SeedSequence`-based `make_rng(seed, stream="")` Generator factory with named substreams; the sole sanctioned randomness source repo-wide.
- **lib/ vendoring**: 8 modules copied byte-identical from `msproject_misc` (HEAD `e723f028`; `continual/{evaluation,stream}.py` capture the uncommitted-edit working-tree state the reference pins were reproduced against). Every blob hash verified against ASSETS §2. Inventory + read-only policy: `lib/PROVENANCE.md`.
- **Tests (7 passing)**: `test_config_roundtrip`, `test_config_rejects_unknown_key`, `test_config_is_frozen`, `test_rng_determinism`, `test_repo_layout` (asserts the roots.env data-access deviation: no `data/embeddings/`), `test_no_reference_imports` (vendoring guard), `test_no_learning_in_fpcmc` (frozen-encoder invariant #6, seeded at T0).
- **Approved deviations**: standalone `hdbscan` PyPI package omitted from the TASKS T0 dep list (ported code uses `sklearn.cluster.HDBSCAN`, ASSETS §4); noted in `pyproject.toml`.

## 2026-07-10 — T1: Data layer: embedding I/O + synthetic fixture generator (`task/T01-data-layer`, commits `17f6fa8`..`881c892`)

- **Dependency**: `torch==2.11.0` added, matching the source venv that produced `tests/reference_numbers.yaml` (`2.11.0+cu126`); served from the PyTorch CPU-only wheel index (`[tool.uv.sources]`) since torch is used solely to `torch.load` the `.pt` files and `torch.load` is build-independent — avoids ~3GB of CUDA wheels in a CPU-only project (PRD §2.2).
- **Data layer**: `fpcmc/data.py` — `roots.env` parsing (`${VAR}` expansion), `EMBEDDINGS_DIR` resolution, five-pools/four-files `PoolSpec` table (`real_cifar100.pt` split by `sources`), `torch.load` with mmap where possible, idempotent on-load L2 normalization (already-unit input returned bitwise-unchanged), full schema validation raising `DataError` (CLAUDE.md stop condition), `embeddings_available()` implementing the documented skip condition (roots.env missing/unset or resolved files absent — never a literal `data/embeddings/` check). File-scoped `label_mappings` respected: integer ids are per-file; cross-pool joins on class-name strings only (ASSETS §1).
- **Fixture world**: `tests/fixtures/vmf_world.py` — analytic vMF universe in D=32: exact equiangular mean construction (configurable separation, supported range (0°, 90°]), per-class κ, Wood (1994) vMF sampler, one-off distractor outliers, `make_stream(schedule)` over shuffled/contiguous `Segment`s, analytic helpers (`true_mean(s)`, `true_pairwise_angles_deg`, `kappa`). All randomness through `make_rng(seed, named substream)`; sampling is a pure function of (seed, stream label, class, n) — no hidden state.
- **Golden stream**: `tests/fixtures/golden_stream.py` + committed `golden_stream.npz` (~1.2MB, sha256 pinned: `4c3670a0…c2fa`). Frozen world seed=7, 8 known + 3 novel (each recurring across ≥4 of 8 `window_W=250` windows, 25/appearance, with post-promotion-feasible windows remaining) + one κ=500 burst class in a single contiguous 15-example run mid-window-3; 2,000 steps total (T8's burst scenario length). The `.npz` also freezes the T0 init pool (200/class), IND test pool (50/class), true means/κ, and per-step window/segment metadata, so T11's LTM init is byte-stable too.
- **Tests (12 passing = 7 prior + 5 new)**: `test_fixture_determinism`, `test_fixture_separations`, `test_l2_on_load` [U], `test_golden_stream_frozen` [U] (hash pin + seed-regeneration content equality + structural spec), `test_real_pool_schemas` [I] — **ran green against live data** at `EMBEDDINGS_DIR` (counts 50,000/10,000/250/500/2,576, D=1024, label alignment, no NaN/Inf; full suite 1.35s, fast loop 0.62s). `test_repo_layout` extended additively with the T1 artifacts.
- **Approved deviations**: 25 one-off distractor outliers (`distractor_00`..`distractor_24`, each appearing exactly once, all after the burst) added to the golden stream beyond TASKS T1's enumerated composition — owner-approved 2026-07-10 — so the burst class's LRU eviction (T8 `test_outlier_burst_never_promotes`, T11 golden gate) is reachable by design under a golden-run config with `stm_capacity ≤ ~25`, independent of emergent threshold-tail behavior.

## 2026-07-10 — T1 follow-up: owner-directed spec/doc sync (`task/T01-data-layer`)

Owner instructed post-T1 review of `docs/` for staleness affecting the remaining tasks; all edits are additive, dated as-built annotations (no assertion weakened, no requirement changed). This entry is their record.

- **TASKS.md**: T0 scope + T1 `test_real_pool_schemas` — annotated the superseded `data/embeddings/` wording with the decided roots.env mechanism and the real skip condition. T1 golden-stream scope — as-built composition (25 planted distractors, frozen T0/test pools + metadata, pinned sha256). T8 `test_outlier_burst_never_promotes` and T11 `test_golden_stream_end_to_end` — noted the distractors and that the golden-run config needs `stm_capacity ≤ ~25` for LRU pressure; distractors enter no metric denominators. T12 — pointed the P2 scope and `test_p2_real_partition` at the frozen `configs/p2_class_split.yaml` list (aligns TASKS with CLAUDE.md source-of-truth #5).
- **PRD**: §3 — noted paths/schemas are filled in `docs/ASSETS.md` with roots.env access. §9 — noted the owner-confirmed top-level-sibling layout as built at T0 (plus `lib/`, `reference/`, `docs/`, `data/`).
- **CLAUDE.md**: fixed stale "config-load time" (data resolution happens at load time; named the loader/skip helpers); added the golden-stream-freeze rule (never regenerate/re-pin without owner approval; `stm_capacity ≤ ~25` note); added `torch==2.11.0` to the pinned-deps line with the no-learning caveat; added the fixture-world usage pointer; clarified the specs-immutable exception wording (CHANGES.md appends standing; other `docs/` edits only on explicit in-session owner instruction, recorded here).

## 2026-07-10 — T2: Per-concept scorers (`task/T02-scorers`, commits `2361e19`..`328488b`)

- **Concept stub**: `fpcmc/concepts.py` — scoring-relevant slice of FR-1's `Concept` (`concept_id`, `centroid`, `ref_set`, `tau`, `kappa`, `tau_vmf`); completed in T3.
- **Scorers**: `fpcmc/scorers.py` — frozen T2 interface `Scorer.{score, accepts, margin, score_detail, select}` (stateless, RNG-free, deterministic pure functions of `(z, concept)`; inputs assumed L2-normalized per FR-1.2, so T5 can add a vectorized batch path without interface change). `KnnRefScorer` (mean cosine distance to `min(k_ref, K)` NN via `np.partition`; works from ref_set size 1). `VmfScorer` (score = −(log C_D(κ) + κ·μᵀz) against `tau_vmf`; below `n_vmf_min` delegates wholesale to knn_ref against `tau` with `fallback=True` in the `ScoreDetail` metadata). `KnnVmfScorer` (FR-4.3 OR-accept; per-concept margin = best sub-scorer margin, `via` records which). `select()` implements best-margin assignment with exact ties broken by lexicographic `concept_id`, input-order independent. `make_scorer(config)` factory. vMF numerics (Banerjee κ with r̄ clip, A&S 9.7.7 uniform-asymptotic log-Bessel as the production path at every D, log C_D with near-uniform guard) adapted with citations from `lib/evaluation/continual/paradigms/vmf_dpmm.py` (blob `47d7412f`, read-only); the source's `kappa_min`/`kappa_max` clips dropped (no PRD §8 keys).
- **Tests (23 passing = 12 prior + 11 new)**: the 8 TASKS-T2 [U] tests — `test_kappa_recovery` (κ∈{20,100,500}, D=32, n=500, ≤15% rel. error, 5 seeds; parametrized ×3), `test_kappa_monotone`, `test_knn_ref_monotonicity` (5 geodesic waypoints, strict), `test_knn_ref_small_refset` (sizes 1–4, exact clipped-k values), `test_vmf_fallback` (pytest-mock spy + metadata flag; NaN κ/τ_vmf proven untouched on the fallback path), `test_composed_or_logic`, `test_composed_assignment_margin` (margin ordering + exact-tie lexicographic break both orders + reject-all → `None`), `test_scorer_determinism` (all three scorers, two fresh instances, exact float equality) — plus `test_vmf_survives_high_dimension` (extra, load-bearing for T6: κ estimation + vmf score finite at D=1024, where `scipy.special.ive` underflows). `test_repo_layout` extended additively. Fast loop 0.66s; full suite (incl. real-data [I]) 1.27s.
- **Approved deviations (owner Q&A, 2026-07-10, pre-implementation)**: (1) `Concept` carries per-sub-scorer thresholds `tau` (knn_ref; also the FR-3.2 seeding/single-scorer threshold) + `tau_vmf` (vmf), since FR-4.3's "respective per-concept threshold" cannot be one scalar across the two score scales — FR-1 declares a single `tau: float`; cascades to T4 (LOO percentiles and the FR-5.3 global prior become per-sub-scorer). (2) Assignment margin normalized as `(τ−s)/|τ|`, not FR-4.3's literal `(τ−s)/τ`, which inverts orientation for the negative vmf thresholds at D=1024; identical to the PRD formula for all τ>0. (3) `KnnVmfScorer.score` scalar = the knn_ref sub-score (FR-4.3 defines accept/assign but no composed scalar) — mirrors the pure-kNN detection statistic of the source paradigm behind the T6 pin; consequently composed `accepts` is not derivable from `score` alone (documented).

## 2026-07-10 — T2 follow-up: owner-directed spec/doc sync (`task/T02-scorers`)

Owner instructed a post-T2 review of `docs/` and `CLAUDE.md` for staleness affecting the remaining tasks; all edits are additive, dated as-built annotations (no assertion weakened, no requirement changed). This entry is their record.

- **PRD**: FR-1 — noted the owner-approved `tau`+`tau_vmf` per-sub-scorer threshold split on the Concept dataclass. FR-4.3 — noted the `(τ−s)/|τ|` margin normalization + lexicographic tie-break and the composed scalar = knn_ref sub-score decision. FR-5 (after FR-5.4) — noted that FR-5.1–5.4 (LOO, shrinkage, global prior, promotion recompute) apply per sub-scorer under `scorer=knn_vmf`, and FR-3.2 seeding bootstraps both thresholds.
- **TASKS.md**: T2 — as-built frozen interface (`score/accepts/margin/score_detail/select`) + the three approved deviations. T3 — preserve `tau_vmf`; seed both taus from the per-sub-scorer priors; keep cached κ valid (`VmfScorer` raises on non-finite κ at ref_set ≥ n_vmf_min); flagged the κ update cadence (per-observation vs T4's lazy trigger) as PRD-unspecified — T3 must raise it before implementing. T4 — LOO/shrinkage/prior per sub-scorer. T5 — consume `Scorer.select()`/`score_detail()` (or match their exact semantics in the vectorized path); `via`/`fallback` metadata feeds the event log and A5. T6 — init populates `tau`/`tau_vmf`/`kappa` + prior pair; clarified the M1 novelty statistic (composed scalar = knn_ref sub-score).
- **CLAUDE.md**: added the frozen-scorer-interface bullet (interface surface, the three deviations, per-sub-scorer T4 machinery, A&S 9.7.7-only log-Bessel rule, finite-κ contract).

## 2026-07-10 — T3: Concept dataclass, reservoir reference sets, centroid dynamics (`task/T03-concept-dynamics`, commit `fec57dc`)

- **Concept completed**: `fpcmc/concepts.py` — the T2 stub (`concept_id`, `centroid`, `ref_set`, `tau`, `kappa`, `tau_vmf`, all meanings preserved) gains the FR-1 remainder: `status`, `ref_count_seen` (defaults to `len(ref_set)` on direct construction), `match_count`, `match_windows` (`step // window_W` indices), `created_at`, `last_matched_at`, `provenance`, `gt_majority_label` (eval-only), `merged_from` lineage list (FR-1.4; maintained by T9), plus construction-fixed operational fields `window_W`/`k_max`/`alpha_ema` and the per-concept reservoir `rng`. `concept_id` is immutable — reassignment/deletion raise `AttributeError` via `__setattr__`/`__delattr__` guards (invariant 4).
- **Dynamics**: `add_observation(z, step)` (TASKS-literal signature) — match/window/LRU bookkeeping; FR-1.1 reservoir (append below `k_max`; once full, exactly one `rng.random(2)` pair per observation: replace iff `u < k_max/ref_count_seen` at slot `int(v*k_max)` — uniform since 64 | 2⁵³; draw discipline documented as load-bearing for the uniformity test's exact vectorized replay); FR-1.3 centroids (STM: `normalize((1−α)c + αz)` every step; LTM: bit-frozen while reservoir/bookkeeping still move, which FR-8.2 relies on); cached Banerjee κ recomputed via `fpcmc.scorers.estimate_kappa` (deferred import; scorers imports us) on every ref_set mutation, so `VmfScorer`'s finite-κ contract holds from birth. `Concept.seed(z, step, tau_prior, tau_vmf_prior=NaN, *, concept_id, rng, window_W, k_max, alpha_ema)` — FR-3.2 singleton (`ref_set=[z]` owned copy, `centroid=z`, status STM, `provenance="seeded"`, both thresholds bootstrapped from the per-sub-scorer priors per the PRD FR-5 note).
- **Tests (31 passing = 23 prior + 8 new)**: the 7 TASKS-T3 [U] tests — `test_reservoir_uniformity` (2,000 trials × 1,000 items into K_max=64 via an exact vectorized replay of the draw discipline, asserted bit-equal to the real `add_observation` path on trial 0 before trusting the statistics; χ²(df=999)=858.9, p=0.9995 ≫ 0.01; 3 items outside 3σ, bounded ≤ 15 — see deviation (5)), `test_reservoir_bound_and_count` (500 steps, K_max=16, per-step bound + exact `ref_count_seen`), `test_ema_stm_centroid` (hand-arithmetic 3-step chain, atol 1e-8, unit-norm every step), `test_ltm_centroid_frozen` (`tobytes()` identity over 100 observations, reservoir churn proven), `test_match_windows` (steps 10/240/510/1260 at W=250 ⇒ `{0,2,5}`, match_count 4), `test_seed_singleton` (full seed shape incl. κ validity, array ownership, NaN `tau_vmf` default), `test_concept_id_immutable` (set + del raise; `status` stays mutable for T8) — plus `test_concept_id_immutability_invariant` in `tests/test_invariants.py` (invariant 4's immutability half; run-wide uniqueness lands with T5's ConceptStore). Fast loop 1.01s; full suite (incl. real-data [I]) 1.70s. No layout change (no new `fpcmc/` files).
- **Approved decisions (owner Q&A, 2026-07-10, pre-implementation)**: (1) κ cadence — recompute on every ref_set-changing observation (cheap at K≤64; T4's lazy ≥25% trigger governs τ only), settling the question TASKS T3 flagged. (2) Seed-count semantics — the seeding embedding counts in `ref_count_seen` (it occupies the reservoir) but not in `match_count`/`match_windows`; `last_matched_at = created_at = step` so LRU is defined from birth. (3) Plumbing — the concept carries its reservoir Generator (per-concept named substream) and the `window_W`/`k_max`/`alpha_ema` scalars, keeping `add_observation(z, step)` literal; `seed()` gains keyword-only `concept_id`/`rng`/config params beyond the TASKS-stated positional three (+ the T2-mandated `tau_vmf_prior`). (4) `provenance` widened to `{"initial", "seeded", "promoted"}` — FR-1's two-value Literal names no value for an unpromoted STM candidate, and FR-8.3's never-merge-two-"initial" rule must not confuse candidates with T0 concepts; T8 flips "seeded"→"promoted". (5) Uniformity-test reading (implementation-side, documented in the test): the literal "every item within 3σ" clause is statistically self-defeating (a perfectly uniform reservoir leaves ~2.7 of 1,000 items outside 3σ, failing ~93% of the time); the TASKS parenthetical χ² p > 0.01 is the binding assertion, with the 3σ-outlier count bounded at 15 (~7× the null expectation) to still catch per-item bias.

## 2026-07-10 — T3 follow-up: owner-directed spec/doc sync (`task/T03-concept-dynamics`)

Owner instructed a post-T3 review of `docs/` and `CLAUDE.md` for staleness affecting the remaining tasks; all edits are additive, dated as-built annotations (no assertion weakened, no requirement changed). This entry is their record.

- **PRD**: FR-1 — as-built T3 note: `provenance` widened to `{"initial", "seeded", "promoted"}` (FR-8.3 unaffected); seed-count semantics (seed counts in `ref_count_seen`, not `match_count`/`match_windows`; maturity/θ are post-seed matches; `last_matched_at = created_at` at seed); per-observation κ cadence; construction-fixed operational fields (per-concept reservoir Generator, `window_W`/`K_max_refset`/`alpha_stm_ema`); `merged_from` as the survivor-side list form of the FR-1.4 lineage map; `concept_id` mutation raises.
- **TASKS.md**: T3 — as-built note recording the four owner-approved decisions, the extended `seed()` signature, and the uniformity-test reading (χ² p > 0.01 binding; literal per-item 3σ fails a correct implementation ~93% of the time, so the 3σ-outlier count is bounded instead). T4 — κ cadence settled (dirty counter and lazy trigger govern τ/τ_vmf only; the counter is a new additive `Concept` field, deliberately not added at T3; `seed` already accepts the prior pair). T5 — the store allocates concept_ids and supplies the per-concept reservoir substream + config scalars at seed; maturity means n_mature post-seed matches; LRU defined from birth. T8 — atomic promotion also flips `provenance` "seeded"→"promoted"; size-θ counts post-seed matches.
- **CLAUDE.md**: added the T3 Concept-dynamics bullet (κ self-maintenance, seed-count semantics, self-contained concept plumbing, provenance values, array ownership, and the load-bearing reservoir draw discipline tied to `test_reservoir_uniformity`'s exact replay).

## 2026-07-10 — T4: Per-concept adaptive thresholds (`task/T04-thresholds`, commit `b381049`)

- **Thresholds module**: `fpcmc/thresholds.py` — per the T2 threshold split, all FR-5 machinery runs per sub-scorer (knn_ref → `tau`, vmf → `tau_vmf`). `loo_knn_scores` (FR-5.1 LOO: pairwise `1 − R@Rᵀ` with the self-distance masked to `+inf`, mean of the `k = min(k_ref, K−1)` smallest via `np.partition`; duplicates legitimately score 0 against twins; requires K ≥ 2); `loo_vmf_scores` (members scored against the cached (centroid, κ) via the same `−(log C_D(κ) + κ·μᵀz)` the frozen `VmfScorer` computes; raises on non-finite κ); `tau_empirical` (`np.percentile`, default linear interpolation — pinned by mirroring `lib/evaluation/continual/paradigms/knn_vmf.py::_calibrate_tau`, as is the self→inf LOO discipline); `shrinkage_weight`/`shrink` (FR-5.2, `w = n/(n+n_shrink)`, n = ref_set size; n=0 returns the prior exactly even against NaN τ_emp); `compute_global_prior(concepts, config) -> GlobalPrior` (FR-5.3: per-concept LOO scores pooled over all T0 concepts, one percentile per sub-scorer; frozen dataclass of plain floats — structurally immutable after T0); `recompute_thresholds` (LTM → pure FR-5.1 percentile; STM → FR-5.2 shrinkage; resets the dirty counter); `maybe_recompute` (lazy FR-5.1 trigger, `REFSET_CHANGE_FRACTION = 0.25`); `recompute_on_promotion(concept, config)` (FR-5.4: pure FR-5.1 from the full ref_set for both taus, status-independent — T8 flips status around this call; raises below K=2, unreachable given FR-7's θ criterion).
- **Concept**: additive `refset_changes_since_tau: int = 0` field (the TASKS-T4 dirty counter, deliberately deferred from T3) — incremented in `add_observation` exactly where a real ref_set mutation happens (same `changed` guard as the κ recompute), read/reset by `fpcmc.thresholds`. No other T3 semantics touched; all 31 prior tests unmodified and green.
- **Tests (38 passing = 31 prior + 7 new)**: the 7 TASKS-T4 [U] tests — `test_loo_hand_case` (5-point 2-D case, k_ref=1, hand NN arithmetic; q=75 exact order statistic + default q=95 vs `np.percentile` of the hand scores, atol 1e-15 for BLAS dot ordering), `test_loo_excludes_self` (duplicate rows: twins legitimately 0, a distinct member must not collapse to its self-distance at k_ref ∈ {1, 2, clipped}; singleton raises), `test_shrinkage_limits` (n=0 ⇒ prior exact; n=10⁴ within 1e-3·τ_emp; w(n_shrink)=0.5 exact + midpoint), `test_prior_fixed_after_t0` (heavy post-prior concept mutation + recomputes leave the stored pair bit-identical; `FrozenInstanceError` on assignment; identical pair from identically rebuilt concepts), `test_lazy_recompute_trigger` (K_max=64, below-capacity appends so mutations are deterministic: 15 → no fire at check (15 < 0.25·62), 17 → fires (17 ≥ 0.25·64), pytest-mock spy on `recompute_thresholds`, counter resets, no re-fire), `test_threshold_separates_fixture` (κ=200, separation_deg=60, full FR-5.3→FR-5.1 stack, composed knn_vmf acceptance: same-class 0.928 ≥ 0.90, other-class rejection 1.000 ≥ 0.99 — comfortable headroom, deterministic draws), `test_promotion_recompute` (a far prior makes the n=32 STM shrinkage visible; the hook lands both taus on the pure percentiles and resets the counter). `test_repo_layout` extended additively. Fast loop 1.04s; full suite (incl. real-data [I]) 1.75s.
- **Approved decisions (owner Q&A, 2026-07-10, pre-implementation)**: (1) vmf LOO scores members against the concept's cached (centroid, κ) — no per-member re-fit; "against the concept" as runtime queries face it, vectorizable, no K−1 < n_vmf_min edge; small-n self-inclusion optimism is what FR-5.2 shrinkage damps. (2) Below the computable floor thresholds hold the prior: τ at n=1 set to τ_prior exactly; τ_vmf recomputed only once ref_set ≥ n_vmf_min (unread in fallback mode below it, keeps its seeded prior). (3) Dirty counter counts actual ref_set mutations (post-fill skipped draws don't count); trigger `counter ≥ 0.25 × current ref_set size`, evaluated lazily on `maybe_recompute()` call (T5 picks the call site), reset on every recompute — reconciling FR-5.1's "≥ 25%" with TASKS' 15-no/17-fires test literal (no check runs at 16). The hand-off's percentile-method question needed no decision: mirrored from the lib batch pipeline (default linear `np.percentile`) with citation. Minor signature note: `recompute_on_promotion(concept, config)` extends TASKS' `recompute_on_promotion(concept)` with the config (q/k_ref/n_vmf_min source), same pattern as the T3 `seed()` plumbing.

## 2026-07-10 — T4 follow-up: owner-directed spec/doc sync (`task/T04-thresholds`)

Owner instructed a post-T4 review of `docs/` and `CLAUDE.md` for staleness affecting the remaining tasks; all edits are additive, dated as-built annotations (no assertion weakened, no requirement changed). This entry is their record.

- **PRD**: FR-5 — as-built T4 note: vmf LOO against the cached (centroid, κ) with self-exclusion confined to knn_ref; below-floor hold-prior rule (τ at n=1 = τ_prior exactly; τ_vmf only from n_vmf_min); FR-5.1 vs FR-5.2 select on `status` (LTM pure percentile / STM shrinkage at every n); dirty-counter semantics (actual mutations vs current ref_set size, check-on-call via `maybe_recompute`, reset on every recompute, τ/τ_vmf only); percentile = `np.percentile` default linear mirrored from `lib/.../knn_vmf.py::_calibrate_tau`; FR-5.4 hook = `recompute_on_promotion(concept, config)`, status-independent.
- **TASKS.md**: T4 — as-built note recording the three owner-approved decisions, the self-resolved percentile question, the module surface, and the one-arg→two-arg `recompute_on_promotion` signature extension (T3 `seed()` precedent). T5 — the store holds the frozen `GlobalPrior` (construction-supplied; T6 init produces it in production), seeds with `prior.tau`/`prior.tau_vmf`, and picks the `maybe_recompute` call site; `recompute_thresholds` is status-sensitive so the store needs no special-casing. T6 — `compute_global_prior(concepts, config)` is the FR-5.3 entry point; LTM-status recompute = pure FR-5.1; T0 concepts need valid cached κ before the vmf side. T8 — atomic promotion's FR-5.4 recompute is `recompute_on_promotion(concept, config)`, never the status-sensitive path. T9 — merges replace ref_set outside `add_observation`, so the merge site recomputes κ itself (`fpcmc.scorers.estimate_kappa`) before τ via `recompute_thresholds` (which resets the dirty counter).
- **CLAUDE.md**: added the T4 thresholds bullet (per-sub-scorer FR-5 stack surface, cached-(μ,κ) vmf LOO, pinned percentile method, status-sensitive `recompute_thresholds` vs status-independent `recompute_on_promotion`, check-on-call lazy trigger + dirty-counter semantics, frozen `GlobalPrior`, and the recompute-κ-yourself rule for wholesale ref_set replacement).

## 2026-07-11 — T5: ConceptStore and routing core (`task/T05-concept-store`, commit `c00c3db`)

- **Routing result**: `fpcmc/concepts.py::RoutingResult` — frozen record of one FR-9 decision: `prediction` (winning concept_id at tier 1; `"unknown"` at tiers 2/3 per FR-9.1), `concept_id` (assigned or seeded), `tier` (1/2/3), `score` (the winning ScoreDetail scalar — under knn_vmf the composed scalar = knn_ref sub-score per the T2 decision), `margin`, plus `via`/`fallback` (the T2 ScoreDetail metadata the event log and A5 ablation need). Tier 3 carries `score = margin = NaN`, `via = None` (nothing accepted; the seed never scored itself).
- **ConceptStore**: `fpcmc/concepts.py::ConceptStore(config, prior, concepts=(), *, vectorized=True)` — holds the frozen FR-5.3 `GlobalPrior` pair (construction-supplied; T6 init produces it in production) and the concept registry. `ltm`/`stm`/tier membership are live views over `concept.status`/`match_count` at route time, so a T8 promotion (or the T5 test's manual flip) participates in tier-1 routing on the very next call (FR-5.4 promotion-aware routing). `route(z, step)` implements exactly the FR-9 cascade: (1) LTM ∪ mature-STM (`match_count ≥ n_mature`, post-seed matches per T3) → best-margin assignment; (2) immature STM → assignment, prediction `"unknown"`; (3) seed new STM singleton via `Concept.seed` with the per-concept reservoir substream `make_rng(config.seed, f"reservoir/{concept_id}")` and the config scalars (T3 decision 3). Store-owned id allocation: `new_concept_id(kind)`; `register` enforces run-wide id uniqueness (invariant 4) and advances the counters past externally allocated ids.
- **Batch scoring path**: `_select_batch` mirrors the frozen scorer math per concept (`1.0 - ref_set @ z`, the FR-4.2 fallback rule, FR-4.3 OR-accept/best-sub-margin with exact ties preferring the knn detail) and vectorizes composition + selection (max margin; exact cross-concept ties to the lexicographically smallest concept_id) over concept arrays. `vectorized=False` routes through the frozen `Scorer.select` loop — the reference `test_vectorized_matches_loop` guards against.
- **Tests (49 passing = 38 prior + 11 new)**: the 7 TASKS-T5 [U] tests — `test_routing_tier_order` (better-margin immature cannot claim from a mature-STM concept; premise margins asserted), `test_routing_tier2`, `test_routing_seeds` (full seed shape: both priors, plumbing scalars, id sequence stm_0000/stm_0001, NaN result fields), `test_promoted_participates_immediately` (tier 2 pre-flip, tier 1 with real prediction on the very next route after `recompute_on_promotion` + status flip), `test_route_updates_bookkeeping` (all mutated fields on exactly one concept; the other bitwise-untouched; LTM centroid frozen; no seeding), `test_vectorized_matches_loop` (parametrized over all three scorer configs; 200 mixed queries exercising all tiers, per-result and final-store bitwise equality, plus a crafted twin-concept exact-tie case since random queries never tie), `test_routing_determinism` (500-query replay ×2, identical result sequences and final state) — plus `tests/test_invariants.py` extensions: `test_concept_id_uniqueness_invariant` (invariant 4's uniqueness half: duplicate registration raises, allocator never re-issues, heavy-novelty stream stays duplicate-free) and `test_no_global_threshold_in_decision_cascade` (invariant 5, AST-enforced: within `ConceptStore` only `__init__`/`_assign`/`_seed` may reference the prior; `fpcmc/scorers.py` is entirely prior-free). No new `fpcmc/` file (store lives in `concepts.py` per TASKS), so no `test_layout` change. Fast loop 1.31s; full suite (incl. real-data [I]) 1.96s.
- **Approved decisions (owner Q&A, 2026-07-11, pre-implementation)**: (1) `RoutingResult.score` = the winning ScoreDetail scalar with `via`/`fallback` carried alongside; tier 3 logs NaN score/margin, `via=None`. (2) FR-5.1 lazy-check call site: `fpcmc.thresholds.maybe_recompute` after every assignment's `add_observation`, matched concept only, tiers 1 and 2 alike; no store-wide sweeps at T5; tier-3 seeds skip it (fresh counter). (3) Every seeded singleton gets `tau = prior.tau` AND `tau_vmf = prior.tau_vmf` unconditionally (the GlobalPrior is always a full pair) — NaN never enters a routed concept under any scorer config. (4) Concept ids are PRD-literal zero-padded `ltm_{:03d}`/`stm_{:04d}`, store-owned monotone counters, never reused; overflow raises rather than widening (preserving lexicographic = numeric ordering, which the FR-4.3 tie-break makes behavior-relevant; `"ltm_" < "stm_"` resolves exact cross-status ties LTM-first); promotion never re-keys (FR-1.4). (5) Measured mid-task and owner-approved: TASKS' "single matrix op per tier" literal is bitwise-incompatible with the frozen per-concept scorer math (a stacked GEMV's per-row summation order depends on row position in the stack; ~half of row dots differ by 1 ulp at every D incl. 1024) — the batch path therefore computes per-concept GEMVs (still the FLOP-dominant op, no per-row Python work; NFR-1 comfortably met) and vectorizes only composition/selection, keeping `test_vectorized_matches_loop`'s identity guard binding as the TASKS annotation directs.

## 2026-07-11 — T5 follow-up: owner-directed spec/doc sync (`task/T05-concept-store`)

Owner instructed a post-T5 review of `docs/` and `CLAUDE.md` for staleness affecting the remaining tasks; all edits are additive, dated as-built annotations (no assertion weakened, no requirement changed). This entry is their record.

- **PRD**: FR-9 — as-built T5 note: `ConceptStore.route(z, step) -> RoutingResult` surface (prediction/tier/score/margin semantics incl. NaN at tier 3, `via`/`fallback` metadata); live `status`/`match_count` tier views as the FR-5.4 promotion-aware-routing mechanism; the sole `maybe_recompute` call site (after every assignment, tiers 1 and 2); store-allocated zero-padded never-reused ids with overflow raising; the per-concept-GEMV batch path and the measured bitwise incompatibility of a stacked GEMV; the route-mutates / score-read-only distinction the T6 M1 gate must respect.
- **TASKS.md**: T5 — as-built note recording the five owner-approved decisions, the store surface, and the structural invariant-5 rule (prior reachable only from `__init__`/`_assign`/`_seed`). T6 — build order (concepts → prior → `recompute_thresholds` → `ConceptStore(config, prior, concepts)`); `register` keeps the allocator consistent with pre-allocated `ltm_{i:03d}` ids; the M1 gate scores pools READ-ONLY via the frozen scorer, never via `route()` (which mutates bookkeeping/reservoirs and would seed on OOD queries). T7 — the registry has no removal operation yet (T7 adds eviction as a store method); ids stay burned after eviction (invariant 4); LRU inputs already maintained (`last_matched_at` per assignment, = `created_at` at seed); tier-3 `_seed` is the only STM growth site (natural capacity-check location); maturity is already a live predicate, not a stored transition. T8 — the atomic flip (status + `recompute_on_promotion` + provenance) is all immediate tier-1 participation requires, proven by T5's `test_promoted_participates_immediately`; STM-accounting release follows automatically if T7 capacity reads the live `stm` view. T11 — `RoutingResult` already carries every `assign`/`seed` record field.
- **CLAUDE.md**: added the T5 ConceptStore bullet (store surface, RoutingResult semantics, live tier views, id-allocation rules, the never-stack-a-cross-concept-GEMV rule with its binding identity guard, structural invariant 5, route-mutates warning); updated the T4 bullet's "the T5 store picks the call site" to the decided call site (after every assignment's `add_observation`).
- Also fixed a missing blank line between the T4-follow-up and T5 entries in this file (formatting only).

## 2026-07-11 — T6 pre-gate: owner-directed re-pin of the `t6_m1_gate` reference (`task/T06-ltm-init`)

The T6 M1 gate came up RED against the 2026-07-10 pin (0.8592). Investigation showed the pin was mis-sourced, not the implementation wrong: the original `t6_m1_gate` entry used the streaming knn_vmf run's whole-stream detection AUROC, but that detector's kNN gallery grows with every promotion mid-stream (detection coupled to discovery), so its whole-stream AUROC is a different quantity from the batch detector the gate is specified against. All edits below are owner-directed (CLAUDE.md specs-immutable standing exception does not cover them; explicit in-session owner instruction, recorded here per the established pattern).

- **Source-side verification run**: fresh STATIC batch knn_vmf detection run in `msproject_misc` (HEAD `e723f028`, all nine pinned blob hashes re-verified byte-identical first) via the new `evaluation/continual/scripts/batch_knn_vmf_static.py` (blob `f17c4194…`; uses the paradigm's own gallery/tau/score code paths verbatim): frozen 50k CIFAR-100-train gallery, promotion/gallery-growth disabled, 20th-NN cosine statistic, IND test (10,000 test + 250 synthetic = 10,250) vs near (500) / far (2,576) / all (3,076) OOD, metrics via the source's own `evaluation._ood_metrics`. Results: **AUROC all/near/far = 0.9410686669415459 / 0.7711480975609756 / 0.9740501439175883**; calibrated tau `0.6165391407194315` is bit-identical to the streaming pin's `threshold_tau` (gallery-identity check). Agrees with the T6 session's static replication of the same statistic to ≤ 1e-4. Command, resolved config, git commit/status, code-path blob hashes, raw per-example scores, and a config snapshot archived at `/home/alex/data/evaluation/dinov3_l/2/batch_knn_vmf_static/{results.json,scores.npz,config_snapshot.yaml}`.
- **`tests/reference_numbers.yaml`** (owner edit — the "never edit" rule binds the coding agent, not the owner): `t6_m1_gate` re-pinned to the fresh static-batch numbers above, with a provenance block (source git hash, exact command + script blob hash, "static batch, 50k-train gallery, growth disabled, 20th-NN statistic", date, archive location, gallery-identity check) and a triage note: T6's instrument (min over 100 per-class concepts, 64-member reservoir ref_sets, mean-5-NN) is a deliberately different statistic expected to agree within the gate tolerance, so a future red gate is triaged as implementation drift vs instrument mismatch. Tolerances kept at ±0.01 / ±0.015 — with the correctly sourced pin the observed instrument gap is Δ = 0.0030/0.0024/0.0031 (all/near/far), so the tolerances bind meaningfully with comfortable headroom. The 0.8592 record was NOT deleted: moved to a new `source_streaming_knn_vmf_run` entry marked "not a gate reference; whole-stream AUROC of a growth-coupled detector; preserved for provenance" — it documents exactly the adaptive-detector effect the F-PCMC work is about. `t14_v1_regression_pin` untouched.
- **Gate status**: `test_m1_gate` GREEN against the re-pin, unmodified — auroc_all=0.938056 (pin 0.941069), near=0.768755 (pin 0.771148), far=0.970918 (pin 0.974050).
- **TASKS.md**: T6 `test_m1_gate` — dated owner note defining "batch knn_vmf pipeline result" as the static, growth-disabled, pre-promotion batch detector and pointing the streaming number at `source_streaming_knn_vmf_run` (closes the ambiguity that mis-sourced the original pin).
- **PRD**: §9 M1 — same definition note ("batch knn_vmf detection AUROC" = static batch, growth disabled, pre-promotion, 20th-NN statistic; not the streaming whole-stream AUROC).

## 2026-07-11 — T6: LTM initialization + M1 sanity gate (`task/T06-ltm-init`, commits `bb7bd9a`..`2ffa64f` + this entry's commit)

- **Init module**: `fpcmc/init.py::initialize_ltm(pool, labels, config) -> ConceptStore` — FR-2.1 in the TASKS post-T5 build order: per-class LTM concepts first (centroid = normalized class mean over ALL class embeddings; ref_set = FR-1.1 reservoir replay over the class pool; κ = `fpcmc.scorers.estimate_kappa(ref_set)`; `status="LTM"`, `provenance="initial"`, `created_at = last_matched_at = 0`, ids `ltm_{i:03d}`), then `compute_global_prior` (FR-5.3), then `recompute_thresholds` per concept (LTM branch = pure FR-5.1 for both taus), then `ConceptStore(config, prior, concepts)` (`register` advances the id allocator past the pre-allocated ids). FR-2.2 honored: no covariance, no precision matrix. Guard: > 1000 classes raises (ltm id-space / FR-4.3 ordering, T5 decision 4).
- **Tests (54 passing = 49 prior + 5 new)**: `test_init_fixture` [U] (8-class world: ids/order, centroids ≤ 3° of true means — worst ~2°, τ finite & positive, τ_vmf/κ finite, FR-2.1 reservoir contract incl. `ref_count_seen` = class count, allocator continuation), `test_init_fixture_refset_members_are_class_samples` [U] (extra: every reservoir row is literally a row of its class pool), `test_init_determinism` [U] (bitwise store equality incl. reservoir contents; different seed ⇒ different reservoirs), `test_m1_gate` [I] (**GREEN**: auroc all/near/far = 0.938056/0.768755/0.970918 vs pin 0.941069/0.771148/0.974050 — Δ = 0.0030/0.0024/0.0031 within ±0.01/±0.015/±0.015; read-only sweep + 50-query spot-check vs the frozen scorer at ≤ 1e-12; store proven untouched), `test_init_runtime` [I] (0.14 s for 50k×1024 vs the 60 s NFR-1 budget, data load excluded). `test_repo_layout` extended with `fpcmc/init.py`. Fast loop 1.35 s; full suite (incl. both real-data [I] tests) 3.88 s.
- **Approved decisions (owner Q&A, 2026-07-11, pre-implementation)**: (1) Init ref_set sampling = the exact FR-1.1 reservoir replay over all class embeddings (append to K_max, then one `rng.random(2)` pair per item, replace iff `u < K_max/ref_count_seen` at slot `int(v*K_max)`) on the per-concept substream `make_rng(config.seed, f"reservoir/ltm_{i:03d}")`, applied directly — never via `add_observation`, which would corrupt the post-seed `match_count`/`match_windows` semantics (T3 decision 2); `ref_count_seen` ends at the full class count so stream-time replacement probabilities continue correctly on the same Generator. (2) `ltm_{i:03d}` follows `np.unique(labels)` order — sorted class names for real data and fixtures alike (canonical; id order feeds the FR-4.3 tie-break). (3) M1 IND side = the pinned run's evaluated population: ind_test (10,000) + synthetic_ind (250) = 10,250, vs near+far OOD (3,076); stratified AUROCs reuse the same IND side vs each OOD pool alone. (4) M1 sweep mechanics: batch across QUERIES with one per-concept `ref_set @ X.T` GEMM (never a stacked cross-concept GEMV; not guaranteed bitwise-equal to the per-query GEMV, which nothing compares against — AUROC is rank-based), helper test-local in `tests/test_init.py`, spot-checked against the frozen per-query scorer; novelty statistic = min-over-concepts composed knn_vmf scalar = knn_ref sub-score (frozen T2 decision).
- **Mid-task stop (gate discipline)**: the gate first ran RED against the 2026-07-10 pin (0.8592) — measured 0.9381, +0.079. Halted per CLAUDE.md (WIP commit `bb7bd9a`, no changelog entry, no spec edits) and reported with diagnostics: a warmup-gallery instrument was ruled out (AUROC ~0.78, τ95 ~0.85), while a static replication of the source detector (50k-train gallery, 20th-NN) matched the pin's recorded τ (0.6153 vs 0.6165) and agreed with the T6 statistic within 0.003 — implicating the streaming run's promotion-grown gallery, not the implementation. Owner confirmed, re-ran the source detector statically, and re-pinned with full provenance (owner edits recorded in the "T6 pre-gate" entry above; streaming number preserved as `source_streaming_knn_vmf_run`). Implementation unchanged by the resolution; tolerances unchanged. **Finding worth keeping: the static batch detector (0.9411) and the growth-coupled streaming detector (0.8592) differ by ~0.08 AUROC on identical data — mid-stream gallery growth from promoted novel clusters depresses later OOD detection scores. That adaptive-detector coupling is a real, measurable effect the F-PCMC evaluation (T11/T13) should expect to see again.**

## 2026-07-11 — T7: STM dynamics: capacity, LRU, maturity (`task/T07-stm-dynamics`)

- **Eviction (FR-3.1)**: `fpcmc/concepts.py` — `EvictionRecord` frozen dataclass (`concept_id`, `size`, `age`, `step`, `created_at`, `last_matched_at`, `ref_count_seen`) and two new `ConceptStore` methods: `_evict_for_capacity(step)` (drain-while over the live `stm` view: while |STM| ≥ `config.stm_capacity`, evict the LRU victim) called at the top of `_seed` — the only STM growth site, per the T5 as-built note — and `_evict(concept, step)` (registry removal + record append to the new public `store.eviction_log`). Victim order: min `last_matched_at`, ties to older `created_at` (TASKS literal), final ties to the smallest `concept_id`. Evicted ids stay burned in `_ids_ever` (invariant 4; `register` unchanged). LTM is exempt regardless of staleness (the `stm` view never contains it); mature-but-unpromoted STM candidates are NOT exempt. Capacity reads the live status view, so a T8 promotion frees its STM slot automatically. Maturity (FR-3.3) needed no new code — tier membership was already a live `match_count ≥ n_mature` predicate at T5; T7 pins the transition behavior in tests.
- **Tests (60 passing = 54 prior + 6 new)**: the 6 TASKS-T7 [U] tests — `tests/test_stm.py`: `test_lru_eviction_order` (Δ=5, crafted last-match steps; insertion #6 evicts exactly the LRU victim; a tier-2 re-match rescues the next would-be victim and the following insertion evicts the now-least-recent instead), `test_lru_tiebreak` (equal `last_matched_at` ⇒ older `created_at` evicted, deliberately anti-correlated with id order; full tie ⇒ smallest id), `test_eviction_log_schema` (3 evictions ⇒ 3 records, exact field-by-field values incl. a mature match_count=9 candidate proving mature-STM evictability), `test_maturity_transition` (match_count = n_mature−1 matches at tier 2; the n_mature-th match flips to tier 1 on the very next route), `test_ltm_never_evicted` (maximally stale LTMs survive while every STM candidate cycles out; eviction log is all-STM) — plus `test_capacity_invariant` placed in `tests/test_invariants.py` because it IS cross-cutting invariant 3 (2,000-step fixture stream, 600 one-off distractors for relentless seeding at Δ=8: |STM| ≤ Δ asserted after every step; books reconcile as live STM = seeds − evictions; LTM count intact). No new `fpcmc/` module ⇒ no `test_repo_layout` change. Fast loop 1.52 s; full suite (incl. real-data [I]) 3.99 s.
- **Approved decisions (owner Q&A, 2026-07-11, pre-implementation)**: (1) `EvictionRecord.size` = `match_count` at eviction — PRD FR-7 criterion 1 literally names `match_count` "Size", so the codebase vocabulary stays consistent. (2) `age` = eviction step − `created_at` (lifetime), with `created_at`/`last_matched_at`/`ref_count_seen` carried beyond the TASKS-literal four fields so T11's JSONL `evict` records and T13's eviction-composition metric need no extra plumbing (staleness derivable). (3) Full LRU ties (equal `last_matched_at` AND `created_at`) evict the smallest `concept_id` (allocation order; consistent with the FR-4.3 id-ordering convention). (4) Capacity mechanics = drain-while loop before the seed lands (normally exactly one eviction per seed; a store hand-built above Δ converges back under it rather than erroring).

## 2026-07-11 — T8: Promotion (`task/T08-promotion`)

- **Promotion machinery**: `fpcmc/memory.py` (new module; T9 adds the MergeSweeper to it) — `PromotionEvaluator(config)` evaluates the four FR-7 criteria against mature STM candidates: size (`match_count ≥ theta_promote`, post-seed matches per T3 decision 2), cohesion (mean pairwise cosine similarity over **distinct** ref_set pairs, self-pairs excluded, ≥ `min_cohesion`), separation (candidate centroid rejected by every LTM concept — see decision 13), recurrence (`len(match_windows) ≥ m_windows`). All four always evaluated (no short-circuit), so every decision names every failing criterion. Atomic promotion (FR-7.1) = status flip → `"LTM"` (freezes the centroid via FR-1.3 and releases the STM capacity slot via the T5/T7 live views — no store surgery) + provenance `"seeded"→"promoted"` (T3 decision 4) + `recompute_on_promotion(concept, config)` (FR-5.4 — the pure-percentile T4 hook, never the status-sensitive path) + a `PromotionRecord` appended to `evaluator.promotion_log` (FR-7.2: step, concept_id, size, cohesion, separation margin, window count, plus eval-only placeholders `gt_majority_label` — copied from the concept, None at runtime — and `purity=None`, filled post hoc by T13; invariant 2 keeps labels out of the pipeline). The evaluator never touches the global prior (invariant 5 stays confined to the store's seed/shrinkage paths).
- **Tests (69 passing = 60 prior + 9 new)**: the 5 TASKS-T8 [U] tests in `tests/test_promotion.py` — `test_each_criterion_blocks` (parametrized ×4 on an orthogonal 4-known/3-novel/1-burst world: 29 matches / two-lobe candidate at ~0.40 mean pairwise sim / known-class clone accepted by its LTM twin / 40 matches in one window; each fails exactly its criterion, no promotion, concept untouched), `test_promotion_happy_path` (single sweep call: status/provenance flips, both taus land bitwise on an unregistered twin's `recompute_on_promotion` values and differ from the pre-promotion shrunk taus, dirty counter reset, |STM|−1/|LTM|+1, centroid bit-frozen under a post-promotion `add_observation`, FR-7.2 record complete field-by-field), `test_separation_uses_per_concept_tau` (same candidate flips blocked→promoted when only the nearest LTM's taus are tightened, and promoted→blocked→promoted when one LTM's knn tau is loosened to 2.0 then restored), `test_outlier_burst_never_promotes` (frozen golden stream through 2,000 steps at `stm_capacity=10`, per-assignment cadence: burst candidate `stm_0087` never promoted — fails size and recurrence — and LRU-evicted at step 954 under the planted-distractor pressure; recurring novelty promoted on the same run, so the discrimination is real), `test_promotion_idempotent` (sweep excludes promoted concepts; direct `check` returns None; log and concept bitwise unchanged) — plus `test_live_sweep_blocks_same_class_fragment` (extra, pins decision 14: with two promotable same-class fragments, ascending-id sweep promotes the first and blocks the second on separation, premise proven by a solo-fragment promotion in a fresh store). `test_repo_layout` extended additively with `fpcmc/memory.py`. Red-first verified (ModuleNotFoundError) before implementing. Fast loop 2.12 s; full suite (incl. real-data [I]) 4.27 s.
- **Approved decisions (owner Q&A, 2026-07-11, pre-implementation; numbering continues from T7)**: (12) Cadence — PRD §8 defines no promotion-schedule key and FR-7.1 says "immediate": the production call site is `check(concept, store, step)` after every tier-1/2 assignment on the just-matched concept (no-ops unless a mature STM candidate; every FR-7 statistic of a candidate changes only on its own matches), with `evaluate(store, step)` as the full sweep for T11's hook machinery and tests. (13) Separation — rejection by every LTM concept under the **configured** scorer with `sep_factor`-scaled thresholds: under knn_vmf both sub-scorers must reject (OR-accept negated; FR-4.2 fallback rule respected per concept), exactly `not accepts()` at the sep_factor=1.0 default; documented caveat that scaling a negative τ_vmf by sep_factor≠1 inverts direction (default 1.0, never swept). Reported separation margin = binding min of `(s − sep_factor·τ)/|sep_factor·τ|` over LTM concepts × applicable sub-scorers (+inf with zero LTM concepts), positive iff the criterion passes. (14) Sweeps are live, ascending concept_id: a promotion is visible to the separation checks of candidates later in the same pass (structural anti-duplicate-promotion guard). (15) Decisions returned, not persisted: `evaluate`/`check` return `PromotionDecision`s (the "log naming the failing criterion" the tests assert); only successes persist on `evaluator.promotion_log`, mirroring `store.eviction_log`.
- **Finding for T9–T11 (not a T8 failure)**: the first promotion-active golden run (stm_capacity=10, per-assignment cadence) promotes novel_00 (step 788, its 3rd window) and novel_01 (step 1082) exactly once each — but novel_02 never promotes, and ~113 STM candidates carried novel-class traffic over the run (heavy same-class fragmentation under the prior-seeded singleton thresholds; 223 evictions). Fragment consolidation is precisely T9 (merging) + T10 (residual clustering); the T11 golden gate ("all 3 promoted, exactly once, unknown-residual < 5%") should not be expected green before those land, and the golden-run `stm_capacity` choice (≤ ~25) may need tuning against fragmentation churn.

## 2026-07-11 — T8 acceptance + owner rulings (owner, in-session)

- T8 accepted as complete: decisions 12–15 confirmed, the extra live-sweep pinning test welcomed, the fragmentation finding validated as expected pre-consolidation behavior (preserved as research signal for the A4 ablation). No reopening.
- **Owner ruling (golden-run config)**: the golden-run config will be a frozen `configs/golden_run.yaml` — hook cadences scaled to the 2,000-step stream, `stm_capacity` chosen for both burst-eviction pressure and fragment coexistence at merge time — created and pinned by the owner before T11's gate first runs; never tuned against gate results.
- **Owner ruling (T10 residual-pool entry)**: immature singletons evicted before `s + w_residual` contribute their seed embedding to the residual pool at eviction time (embedding only — the evicted concept and its id stay dead); survivors enter at `s + w_residual` as specified; matured singletons never enter. TASKS T10 annotated accordingly (owner edit).

## 2026-07-11 — T9: Merging (`task/T09-merging`)

- **Merge machinery**: `fpcmc/memory.py::MergeSweeper(config, prior)` — the FR-8 stack. `sweep(store, step)` runs the periodic pass in PRD order: STM↔STM (FR-8.1 two-condition rule), STM→LTM folds (FR-8.2), LTM↔LTM promoted-only pairs (FR-8.3); each phase iterates ascending-id pairs to a deterministic fixpoint with merges effective immediately. `on_promotion(store, step)` is the FR-8 on-promotion check (LTM↔LTM phase only; wired by T11 after each promotion). `check_pair(a, b) -> MergeCheck` exposes the two-condition evidence (centroid sim, cross/within ratio, compatible). `merge_pair(store, a, b, step, kind=, check=)` applies the FR-8.1 mechanics unconditionally — the public seam T10's HDBSCAN consolidation calls directly, clustering evidence standing in for the two-condition check. Fold trigger = STM centroid **accepted** by an LTM concept under the frozen `Scorer.select` (best-margin accepting LTM, lexicographic tie-break) — the exact complement of T8's separation criterion at sep_factor=1. Every merge site recomputes κ itself (`estimate_kappa`) before the status-sensitive `recompute_thresholds` (T4 rule; LTM survivor centroid stays bit-frozen, ref_set/κ/τ still updated). Survivor = larger `match_count`, ties to smaller `concept_id`. Lineage: `survivor.merged_from += [absorbed, *absorbed.merged_from]` (chained absorption resolvable); `sweeper.lineage` is the FR-1.4 store-level map; every merge appends a `MergeRecord` (step, kind, survivor/absorbed ids, centroid_sim, cross/within ratio — NaN for folds — and match counts) to `sweeper.merge_log`. The sweeper holds the frozen `GlobalPrior` because STM survivors re-shrink toward it and the store cannot expose its own prior without violating invariant 5's AST guard (T11 plumbing note: the runner must carry the prior alongside the store, or propose extending `initialize_ltm` to return the pair). New minimal `ConceptStore.remove(concept_id)` in `fpcmc/concepts.py`: registry removal for absorption, id stays burned (invariant 4), **no** EvictionRecord (absorption ≠ forgetting).
- **Tests (76 passing = 69 prior + 7 new)**: the 6 TASKS-T9 [U] tests in `tests/test_merge.py` — `test_merge_two_conditions` (three crafted D=8 cone pairs with premises asserted via `check_pair`: same-class halves merge; 30°-apart tight twins have sim 0.87 ≥ 0.8 but ratio ≫ 1.1 ⇒ refused — the PRD §11 near-OOD-collapse guard; 60°-apart tight twins fail condition 1 ⇒ refused), `test_merge_survivor_and_lineage` (A(mc50)←B(mc30)←C(mc10) in ONE fixpoint sweep; bookkeeping union asserted exactly; `merged_from == [B, B's own prior lineage, C]`; absorbed ids gone from routing and re-registration raises), `test_stm_ltm_fold` (known-class clone folded into `ltm_000`: centroid bit-identical, `ref_count_seen` 100→140, match stats untouched, union re-bounded to K_max with row-membership proven, κ == fresh Banerjee, τ == pure FR-5.1 recomputed from the new ref_set), `test_initial_initial_never_merges` (two T0 concepts made geometrically identical — `check_pair` compatible as premise — sweep refuses), `test_promoted_promoted_merge` (two manually promoted golden-world novel_00 fragments: LTM↔LTM sweep merges, survivor = larger match_count with frozen centroid, fragmentation index back to 1, all 8 initial concepts intact), `test_merged_refset_bound` (80-row union capped at K_max=64, rows ⊆ union, bitwise-identical across two same-seed runs, different under seed 43) — plus `test_on_promotion_check_merges_immediately` (extra: a lone promoted concept no-ops; a freshly promoted duplicate merges via `on_promotion` without waiting for the periodic sweep). No new `fpcmc/` file ⇒ no `test_repo_layout` change. Red-first verified (ImportError) before implementing. Fast loop 1.78 s; full suite (incl. real-data [I]) 4.23 s.
- **Approved decisions (owner Q&A, 2026-07-11, pre-implementation; numbering continues from T8)**: (16) FR-8.1 condition-2 math — k = min(k_ref, available) throughout; within = pooled mean of both ref_sets' LOO knn scores (frozen `loo_knn_scores` discipline); cross = pooled mean over both directions; merge iff cross/within ≤ 1.1 (`MERGE_CROSS_WITHIN_MAX`, the FR-8.1 literal — deliberately not a §8 key) AND centroid sim ≥ `merge_sim`. (17) Sweep = PRD phase order, each phase to fixpoint, merges live (A←B then A←C in one sweep); `on_promotion` = LTM↔LTM phase only. (18) FR-8.1 merges union the history (match_count sum, match_windows union, last_matched_at max, ref_count_seen sum; survivor keeps id/created_at/status/provenance; STM survivor centroid = normalized mean of the FULL pre-subsample union); FR-8.2 folds move ref_set + ref_count_seen ONLY (a fold is not a match event — LTM match statistics stay honest). (19) Union > K_max is uniformly subsampled without replacement via the dedicated substream `make_rng(config.seed, f"merge/{step}/{survivor}<-{absorbed}")`, original row order preserved; the survivor's per-concept reservoir Generator is never consumed (its FR-1.1 draw discipline stays pure). (20) Pairs with K < 2 on either side are not sweep-mergeable (within-structure unobservable; PRD §11 names the ratio the near-OOD-collapse guard, so err conservative) — singleton consolidation is T10's job via the unconditional `merge_pair` seam.
- **Finding for T10/T11 (not a T9 failure — nothing tuned)**: on the golden stream (stm_capacity=10, T_merge=500, on-promotion checks active) the FR-8.1 sweep fires **zero** merges: at every sweep step live STM is almost entirely K=1 singletons (decision-20 skip), and the one observed K≥2 same-class pair (K=9 main candidate vs K=2 fragment, both novel_00) passes condition 1 (sim 0.857) but fails condition 2 (ratio 1.441 > 1.1 — a 2-member fragment's within-LOO is the distance between two unusually close points). At stm_capacity=25, 3 STM→LTM folds fire but only novel_00 promotes: surviving immature fragments siphon tier-2 traffic away from the main candidate, slowing its march to θ — capacity couples to promotion through traffic-splitting, not just eviction pressure (relevant to the owner's frozen `golden_run.yaml` choice). Burst discrimination (never promoted, always evicted) holds at capacities 10/18/25. Net: golden-scale fragment consolidation rests on T10's residual clustering exactly as designed, and the T11 gate stays red-expected until T10 lands.

## 2026-07-11 — T10: Residual clustering: identity-preserving consolidation (`task/T10-residual-clustering`, commit `e1db541`)

- **Residual module**: `fpcmc/residual.py::ResidualClusterer(config, sweeper)` — the FR-6 assist mechanism, sharing the T9 `MergeSweeper` so the run keeps ONE merge log/lineage map. `note_seed(concept_id, z, step)` records every tier-3 seed's embedding (T11's runner calls it right after the route — the seed embedding exists nowhere else); `hook(store, step)` runs per step: it reconciles the pool lifecycle on every call (evictions discovered via an internal cursor over `store.eviction_log`; a tracked id gone WITHOUT an eviction record was absorbed by a merge), then consolidates only when `step % T_cluster == 0` (step > 0) AND pool ≥ `RESIDUAL_POOL_MIN = 30` (FR-6.1 literal, not a §8 key) — both trigger conditions live in the module, not the runner. Consolidation clusters pool ∪ immature-STM centroids (row layout: pool in (entered_at, id) order, then immature candidates ascending id — exposed as `clustering_membership(store)` for tests/instrumentation); every cluster grouping ≥ 2 alive immature candidates merges them sequentially through the T9 `merge_pair` seam with `kind="residual"` (event-log attributable; lineage recorded; **no concept ids created by this pathway**). The clustering core is the sanctioned ASSETS §2 port of `lib/evaluation/continual/clustering.py::run_sweep`/`_deduplicate_clusters` (blob `55a9ede4`, adapted with citations, never imported): optional UMAP (only when N > `umap.dim`; `n_neighbors = min(cfg, N−1)`; `random_state=config.seed` per the ASSETS §4 determinism pattern), L2-normalize, one `sklearn.cluster.HDBSCAN` fit per §8 `min_cluster_sizes` value (`cluster_selection_method` from §8 `selection`), Jaccard union-find dedup keeping the highest-mean-probability duplicate, final clusters size-descending. `run_log` keeps a `ResidualRunRecord` (step, pool size, immature count, clusters, merges, consumed) per pass. Deferred imports keep umap's numba cost out of mocked unit tests.
- **Tests (84 passing = 76 prior + 8 new)**: the 5 TASKS-T10 tests in `tests/test_residual.py` — `test_pool_aging` [U] (entry exactly at s + w_residual, per-step hooks 11..60; matured-before-aging never enters), `test_trigger_conditions` [U] (mock on `_cluster`: on-schedule at pool 29 → no run; off-schedule at pool 30 → no run; step 0 → no run; on-schedule at 30 → exactly one), `test_identity_preserving_merge` [U] (mocked grouping over 4 immature candidates at match_counts 3/2/1/1: two `kind="residual"` merges via the T9 path, survivors by the T9 rule, lineage on the shared sweeper, ids strictly ⊆ pre-pass ids, clustered pool rows consumed), `test_noise_untouched` [U] (all-noise mock: candidates bitwise unchanged, pool unconsumed, and the LRU-eligibility half proven by an actual eviction at capacity), `test_residual_consolidation_real` [I] (**ran green against live data**, 9.1 s: 3 near-OOD classes → 6 half-candidates (20 members each, match_count 1) + 30 aged singletons; real UMAP(50-D)+HDBSCAN sweep reduces 36 concepts to exactly 3 with correct pairings — each survivor's lineage single-class by ground truth, GT used for verification only) — plus 3 extras: `test_pool_eviction_entry` (pins the owner ruling: evicted-early immature enters at eviction step; mature-evicted never; entry order = eviction order), `test_pool_live_predicate` (absorbed-pending never enters; absorbed-pooled leaves; matured-after-entry leaves), `test_residual_consolidation_and_determinism` (un-mocked sweep+dedup on a crafted two-cone world, N=34 ≤ umap.dim so the HDBSCAN-only branch runs: correct per-cone pairing, full pool consumption, and bitwise-identical merges/ref_sets/thresholds across two identically built runs). Red-first verified (ModuleNotFoundError). `test_repo_layout` extended additively with `fpcmc/residual.py`. Fast loop 1.90 s; full suite (incl. all real-data [I]) 16.95 s — UMAP dominates, comfortably inside NFR-1.
- **Approved decisions (owner Q&A 2026-07-11, pre-implementation; numbering continues from T9)**: (21) Clustering core = faithful port of the lib sweep + Jaccard dedup; the dedup threshold — absent from this repo because the source `ContinualConfig` was never vendored — recovered from the source project (`msproject_misc` @ `e723f028`, `evaluation/continual/config.yaml` `jaccard_dedup_threshold: 0.80`, same default in `config.py`, identical at HEAD/working tree and in the archived T6 config snapshot) and hardcoded as the cited literal `JACCARD_DEDUP_THRESHOLD = 0.80`. (22) Live-predicate pool: the SEED embedding of every seeded-as-singleton candidate ("singleton" = seeded at tier 3, so 1–4 matches still qualify) enters at `created_at + w_residual` iff alive and immature; evicted-early immature enters at eviction time (owner ruling 2026-07-11); absorbed never enters/leaves if in; matured-or-promoted-after-entry leaves at the next hook; evicted-after-entry stays. (23) Retention: pool rows landing in any final (post-dedup) cluster are consumed whether or not a merge fired; noise rows stay. (24) Clusters drive merges only: pool rows are density context (they give singleton structure the mass HDBSCAN's min_cluster_size needs) and never join a ref_set via this pathway; pool-only and single-candidate clusters are no-ops.
- **Discrepancy flagged (docs are owner-only)**: the 2026-07-11 T8-acceptance entry above records "TASKS T10 annotated accordingly (owner edit)" for the eviction-time pool-entry ruling, but `docs/TASKS_frozen_encoder_pcmc.md` carries no such annotation and git shows no TASKS edit after the T6 re-pin — the annotation was apparently never saved. Implemented from the ruling as recorded in this file; the TASKS annotation remains for the owner to (re-)apply.

## 2026-07-11 — T11 pre-gate: owner-directed gate-floor amendment, golden-run config freeze, T10 annotation repair (owner, in-session; `task/T11-stream-runner`)

The T11 StreamRunner landed with its [U]/invariant/runtime tests green but `test_golden_stream_end_to_end` RED, and the coding agent halted per CLAUDE.md gate discipline rather than tuning anything. Diagnosis separated two causes — one spec defect (a gate clause a correct implementation cannot satisfy) and one config choice the T8-acceptance ruling had reserved to the owner. Both are settled here. All edits below are owner-directed (the specs-immutable standing exception does not cover them; explicit in-session owner instruction, recorded here per the established pattern). **Nothing in `fpcmc/` was changed by this entry, and no gate assertion was weakened to accommodate an implementation.**

- **TASKS T11 — golden-gate tier-1 clause amended, ≥ 90% → ≥ 0.85** (owner edit): the clause "post-promotion samples of promoted classes routed at tier 1 … ≥ 90% of that class's post-promotion arrivals" is unsatisfiable-in-expectation as written. The rate has a **structural ceiling at `tau_percentile_q`/100 = 0.95**: FR-5.1 calibrates τ at the q-th percentile of a concept's own LOO scores, so ~5% of that concept's own in-distribution arrivals fall beyond its own τ *by construction* and cannot be accepted by it at tier 1 — the mechanism working perfectly still rejects one arrival in twenty. The smallest post-promotion arrival count on the frozen golden stream is n ≈ 49, so the 3σ binomial band below that ceiling is 3·√(0.95·0.05/49) ≈ 0.093 ⇒ 0.95 − 0.093 ≈ 0.857, floored to **0.85**. The original 90% literal sat *inside the sampling noise of its own ceiling* (≈ 0.5σ below it) — it would fail a correct implementation on draw luck alone. The amendment is dated in TASKS with the derivation and the standing rule that **the floor is q-linked and must be re-derived if `tau_percentile_q` changes**. Note this is a spec defect fixed at the spec, not a pin relaxed at a red gate: the ceiling argument is independent of anything the implementation measured.
- **`configs/golden_run.yaml` — `stm_capacity` 16 → 25, and the file is now FROZEN** (owner edit): the T8-acceptance ruling reserved this file to the owner; the coding agent's 16 is hereby amended to 25 and the config pinned as gate input on the same footing as `golden_stream.npz`. The value is bounded on both sides and **both bounds bind**: it must *exceed* the peak same-class fragment load, so that a candidate coalesced by T10's residual clustering survives the gap windows between its class's appearances (novel classes recur in ≥ 4 of 8 windows) and can actually complete its march to `theta_promote` — at 16, fragment load plus churn evicts the coalesced candidate inside those gaps and the march restarts; and it must stay *below* the demand the 25 planted distractors plus seed churn place on STM, so the burst-eviction assertion has real LRU pressure (the "≤ ~25" of the golden-stream freeze, CHANGES T1). 25 is the largest value satisfying both. Rationale + freeze notice are in the file header. **Consequence: the config is no longer an available degree of freedom — a red gate is now to be answered in `fpcmc/` (or, where the clause itself is defective, at the spec by the owner), never by editing a value here.**
- **TASKS T10 — residual-pool eviction-entry ruling annotated** (owner edit): the dated annotation the T8-acceptance entry claimed was applied is now actually in `docs/TASKS_frozen_encoder_pcmc.md` T10, matching what T10 implemented — an immature singleton evicted before `s + w_residual` contributes its seed embedding to the pool *at eviction time* (embedding only; the evicted concept and its id stay dead per invariant 4); survivors enter at `s + w_residual`; matured singletons never enter. Rationale recorded: under real LRU pressure most singletons are evicted before they can age in, so an aging-only pool would starve the FR-6.1 ≥ 30 trigger of the density context it exists to accumulate.
- **Correction to the 2026-07-11 T8-acceptance entry above** (append-only; the entry itself is left standing as written): its owner-ruling bullet states "TASKS T10 annotated accordingly (owner edit)", but **that annotation was never saved** — no TASKS edit exists in git between the T6 re-pin and this entry. T10 was implemented from the ruling as recorded in this file, and `test_pool_eviction_entry` pins the behavior, so no code is affected; the defect was purely that the spec did not carry a ruling the implementation was correctly following. The T10 entry's "Discrepancy flagged" bullet caught this, and the preceding bullet closes it. Process note for future rulings: a ruling is not applied until the file is written and committed.
- **Gate state after these edits — measured, and STILL RED at clause 5** (reported, not acted on; no `fpcmc/` change and no further spec edit made): clauses 1–4 now pass under the frozen config — all 3 novel classes promoted exactly once (fragmentation index 1.0), burst class zero promotions + LRU-evicted, promoted-concept purity ≥ 0.95, and the amended tier-1 clause clears. **Clause 5 (known-class expanding accuracy ≥ 0.95 at every window end) fails, and diagnosis splits it into two independent causes that need two different owner decisions:**
  1. **The same structural-ceiling defect as clause 4, in a clause the amendment did not touch.** Known-class accuracy is ceilinged at `tau_percentile_q`/100 = 0.95 for exactly the FR-5.1 reason: ~5% of a class's own arrivals fall beyond its own τ by construction and are rejected to tier 2/3. Measured own-rejection rate over the stream = **0.0526**, against the 0.05 the calibration predicts, with **zero** wrong-LTM confusions anywhere in the run. Window ends 1–6 miss the 0.95 floor on this tail *alone* (accuracy 0.9360–0.9465, no traffic theft at all). A ≥ 0.95 floor under a 0.95 ceiling is unsatisfiable in expectation — same defect, same derivation as clause 4.
  2. **A real mechanism defect that a floor amendment would NOT fix — a tier-1 "black hole" concept.** Accuracy *degrades* over the last two windows (0.9362 → 0.9138) because `stm_0130` steals 47 known-class arrivals from their correct LTM concepts in the final 84 steps. It is a mature (`match_count` 66 ≥ `n_mature`) STM candidate seeded from a known class's τ-tail reject, which then accumulated the τ-tail rejects of *many* known classes: GT composition known_02 ×12 / known_07 ×10 / known_03 ×9 / …, **cohesion 0.2075**. Because FR-5.1 calibrates τ at the 95th percentile of a concept's *own* LOO scores, a heterogeneous ref_set earns a pathologically loose **τ = 0.6411, 3.4× the mean initial-LTM τ (≈ 0.148)** — so its `(τ−s)/|τ|` margin outbids every tightly-calibrated LTM concept and it wins tier-1 selection. It is simultaneously **unpromotable** (FR-7 cohesion 0.2075 < `min_cohesion` 0.55 — permanently), **unfoldable** (FR-8.2 needs its centroid accepted by some LTM; its mixed centroid is accepted by none), and **unreachable by residual clustering** (mature ⇒ it left the pool). Nothing in FR-5/FR-7/FR-8/FR-9 bounds an STM candidate's τ or gates mature-STM tier-1 participation on cohesion, so it persists and compounds. **This is a spec gap, not a T11 coding bug — the runner implements FR-9 as written** — so per CLAUDE.md gate discipline it is halted here for an owner decision rather than patched. Candidate levers, none applied: (a) gate mature-STM tier-1 participation on the existing `min_cohesion` key; (b) cap an STM candidate's τ at the global prior τ; (c) let FR-6 residual clustering reclaim low-cohesion mature candidates. The 3σ arithmetic says a ceiling-corrected floor would still not save clause 5's final window (~0.934 required vs 0.9138 observed), which is the cleanest proof that cause 2 is real and independent of cause 1.
